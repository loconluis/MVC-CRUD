@model IEnumerable<MVCManual.Models.Productos>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<div class="table-responsive">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.precio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ultimacompra)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.unidadesinventario)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.precio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ultimacompra)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.unidadesinventario)
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id=item.codigoproducto }) |
                @Html.ActionLink("Detalles", "Details", new { id=item.codigoproducto }) |
                @Html.ActionLink("Eliminar", "Delete", new { id=item.codigoproducto })
                
            </td>
        </tr>
    }

    </table>
</div>
